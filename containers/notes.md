# CLI commands

```bash
docker build -t clonal_analysis .

# one of the two
docker push clonal_analysis  # to upload to Docker Hub
docker save -o clonal_analysis.tar clonal_analysis  # to be shared directly
```
